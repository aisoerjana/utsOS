PRACTICUM 1.1
1. How many processes and how many thread does this program have?
Ans : Prosesnya ada 1 & Thread ada 3 (1 Main, 2 Child)

PRACTICUM 1.2
1. Remove all while(1); statements or infinite loops in prak8-1.c. Compile and run it several times. What do you observe?
Ans : Saat while(1); dihilangkan, program akan berhenti sendiri sebelum 2 thread selesai, sehingga hasilnya akan random, kadang muncul 1, kadang muncul 2

2. Compile and run it several times. What do you observe?
Ans : Program akan berjalan sempurna, karena akan menunggu thread 1 dan thread 2 berjalan terlebih dahulu, barulah program akan berhenti.

3. Can you modify this program so that thread2 always runs after thread1 completes?
Ans : fungsi while(1); diaktifin dua-duanya, lalu di baris akhir, pthread_join(thread2, NULL); dinonaktifkan, sedangkan yang pthread_join(thread1, NULL); diaktifkan dan ditaroh sebelum while(1);

gambarannya seperti ini
pthread_join(thread1, NULL);
while (1);
// pthread_join(thread2, NULL);
return 0;

PRACTICUM 1.3
1. Compile and run it several times. What do you observe?
Ans : ketika program ini dicompile dan run beberapa kali, output dari task1 dan task 2 akan muncul berbeda-beda, ini bisa terjadi dikarenakan Operating System yang menentukan thread mana yang jalan terlebih dahulu. Dalam program ini, thread 1 menjalankan task1, dan thread 2 menjalankan task 2, keduanya berjalan secara bersamaan (paralel). Masing-masing thread menghitung 0-9 lalu mencetak nilainya setiap 1 detik (sleep(1)), setelah selesai task1 akan mengubah global variabel g1 jadi 10 dan task2 menjadi 20

2. Is the final value of g1 always the same each time the program is run? Explain!
Ans : final value dari g1 tidak selalu sama, karena task1 mengubah g1 menjadi 10 dan task2 mengubah g1 jadi 20. Jadi thread yang selesai terakhir adalah yang menentukan nilai akhir g1.

3. Modify the program so that, even though thread1 and thread2 run concurrently, before the program exits the value of the global variable g1 must be 20. Note, the main thread is not allowed to change the value of the global variable g1.
Ans : Saya memindahkan pthread_join(thrd1, NULL); ke atas, jadi program akan menjalankan thread1, lalu menunggu thread1 selesai, barulah program lanjut menjalankan thread2.

PRACTICUM 1.4
1. Compile this program. Before running the program, determine what the final value of cnt should be.
Ans : final value dari cnt adalah 20.000.000, hasil dari 10.000.000 + 10.000.000
// Diketahui NITERS 10 juta
#define NITERS 10000000
// Ada 2 thread
pthread_create(&tid1, NULL, (void *)count, NULL);
pthread_create(&tid2, NULL, (void *)count, NULL);
// Masing-masing melakukan perulangan, thread1 increment 10jt, thread2 increment 10jt
for (i = 0; i < NITERS;  i++)
cnt++;

2. After running, what is the value of cnt?
Ans : Wrong...cnt = 11483496, 12774269, 12443624
Hasilnya berbeda-beda, karena race condition. Hasil juga tertulis wrong karena perintah cnt++, jadi semisal thread1 cnt = 5, thread2 juga akan menghasilkan output cnt = 5 (mestinya 6).

3. Discuss this race condition phenomenon with the lecturer and lab assistant.
Ans : 

